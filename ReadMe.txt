From input.txt we are getting two numbers.

These two numbers is going to sever. Server is making one's compliment and then adding it.
We are getting result in server and sending it client.

To run program use following commands:

make

then following command in server side:

./server

then following command in client side:

./client
